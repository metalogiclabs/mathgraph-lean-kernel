name: msikernel operator alphabet genesis v16

on:
  push:
    branches: [msikernel-operator-alphabet-genesis-v16]
  workflow_dispatch:

jobs:
  operator-alphabet-genesis-v16:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v31

      - name: Build dependency-complete discovery and prospective prefixes
        run: |
          set -euxo pipefail
          git clone --depth 1 https://github.com/leanprover/lean-kernel-arena.git arena
          cd arena
          nix develop -c ./lka.py build-test std
          nix develop -c ./lka.py build-test cedar
          for c in std cedar; do
            head -n 400000 _build/tests/$c.ndjson >/tmp/$c.train.ndjson
            head -n 600000 _build/tests/$c.ndjson >/tmp/$c.test.ndjson
            test "$(wc -l </tmp/$c.train.ndjson)" -eq 400000
            test "$(wc -l </tmp/$c.test.ndjson)" -eq 600000
          done
          cd ..
          cat >/tmp/checker.json <<'EOF'
          {"use_stdin":true,"nat_extension":true,"string_extension":true,"unpermitted_axiom_hard_error":false,"unsafe_permit_all_axioms":true,"num_threads":1,"print_success_message":false}
          EOF
          cp OPERATOR_ALPHABET_GENESIS_V16.md /tmp/prereg.md

      - name: Collect raw anonymous residual observations
        run: |
          set -euxo pipefail
          git clone --depth 1 --branch mathgraph https://github.com/metalogiclabs/mathgraph-lean-kernel.git /tmp/probe
          python3 experiments/operator_alphabet_runtime_v16.py /tmp/probe probe | tee /tmp/runtime-probe.txt
          cd arena
          nix develop -c bash -euxo pipefail -c "CARGO_TARGET_DIR=/tmp/target-probe RUSTFLAGS='-C target-cpu=x86-64' cargo build --manifest-path /tmp/probe/Cargo.toml --release --locked"
          for c in std cedar; do
            /tmp/target-probe/release/sokonanoda /tmp/checker.json </tmp/$c.train.ndjson >/tmp/$c.probe.out 2>/tmp/$c.probe.log
          done
          cd ..
          python3 - <<'PY'
          import re,csv
          hdr=['corpus','event_id','safe','disc_hash','spine_empty','closed','canonical']
          n=0; counts={0:0,1:0}
          with open('/tmp/v16_observations.csv','w',newline='') as f:
              w=csv.writer(f); w.writerow(hdr)
              for corpus in ('std','cedar'):
                  event=0
                  for line in open(f'/tmp/{corpus}.probe.log',errors='replace'):
                      m=re.search(r'APPGEN_V16 safe=([01]) disc_hash=(\d+) spine_empty=([01]) closed=([01]) canonical=([01])',line)
                      if not m: continue
                      vals=list(map(int,m.groups()))
                      w.writerow([corpus,event,*vals]); event+=1; n+=1; counts[vals[0]]+=1
          print(f'V16_PROBE_EVENTS={n} SAFE={counts[1]} UNSAFE={counts[0]}')
          assert counts[1] > 0 and counts[0] > 0
          PY

      - name: Measure initial grammar then license one operator schema
        run: |
          set -euxo pipefail
          python3 experiments/operator_alphabet_genesis_v16.py /tmp/v16_observations.csv | tee /tmp/learn.txt
          grep -q 'V16_INITIAL_LANGUAGE_INSUFFICIENT=PASS' /tmp/learn.txt
          grep -q 'V16_UNIQUE_BEST_OPERATOR_BEHAVIOURAL_CLASS=PASS' /tmp/learn.txt
          grep -q 'V16_EXACT_OPERATOR_ABLATION_FRONTIER=PASS' /tmp/learn.txt
          test -s /tmp/v16_expr.txt
          test -s /tmp/v16_predicate.txt
          test -s /tmp/v16_broad_predicate.txt
          echo "FROZEN_V16_EXPR=$(cat /tmp/v16_expr.txt)" | tee /tmp/result.txt
          echo "FROZEN_V16_PREDICATE=$(cat /tmp/v16_predicate.txt)" | tee -a /tmp/result.txt
          cat /tmp/learn.txt >>/tmp/result.txt
          grep -q 'V16_SEMANTIC_VARIANT_NAMES_EXPOSED_TO_LEARNER=NO' /tmp/runtime-probe.txt
          echo V16_NO_SEMANTIC_NAME_LEAKAGE=PASS | tee -a /tmp/result.txt

      - name: Compile reference, generated guard, and broadening ablation
        run: |
          set -euxo pipefail
          for mode in g3 guard broad; do
            git clone --depth 1 --branch mathgraph https://github.com/metalogiclabs/mathgraph-lean-kernel.git /tmp/$mode
          done
          EXPR="$(cat /tmp/v16_expr.txt)"
          PRED="$(cat /tmp/v16_predicate.txt)"
          BROAD="$(cat /tmp/v16_broad_predicate.txt)"
          python3 experiments/operator_alphabet_runtime_v16.py /tmp/g3 g3
          python3 experiments/operator_alphabet_runtime_v16.py /tmp/guard guard "$EXPR" "$PRED"
          python3 experiments/operator_alphabet_runtime_v16.py /tmp/broad broad "$EXPR" "$BROAD"
          cd arena
          for mode in g3 guard broad; do
            nix develop -c bash -euxo pipefail -c "CARGO_TARGET_DIR=/tmp/target-$mode RUSTFLAGS='-C target-cpu=x86-64' cargo build --manifest-path /tmp/$mode/Cargo.toml --release --locked"
          done

      - name: Exact discovery semantics and broadening ablation
        run: |
          set -euxo pipefail
          for c in std cedar; do
            /tmp/target-g3/release/sokonanoda /tmp/checker.json </tmp/$c.train.ndjson >/tmp/$c.g3.train.out
            /tmp/target-guard/release/sokonanoda /tmp/checker.json </tmp/$c.train.ndjson >/tmp/$c.guard.train.out
            cmp /tmp/$c.g3.train.out /tmp/$c.guard.train.out
            echo "${c^^}_V16_DISCOVERY_BYTE_IDENTICAL=PASS" | tee -a /tmp/result.txt
          done
          set +e
          /tmp/target-broad/release/sokonanoda /tmp/checker.json </tmp/std.train.ndjson >/tmp/std.broad.out 2>/tmp/std.broad.err; rs=$?
          /tmp/target-broad/release/sokonanoda /tmp/checker.json </tmp/cedar.train.ndjson >/tmp/cedar.broad.out 2>/tmp/cedar.broad.err; rc=$?
          set -e
          bad=0
          if [ "$rs" -ne 0 ] || ! cmp -s /tmp/std.g3.train.out /tmp/std.broad.out; then bad=1; fi
          if [ "$rc" -ne 0 ] || ! cmp -s /tmp/cedar.g3.train.out /tmp/cedar.broad.out; then bad=1; fi
          test "$bad" -eq 1
          echo V16_EXACT_GENERATED_BROADENING_ABLATION=PASS | tee -a /tmp/result.txt

      - name: Prospective transfer on untouched rows 400001 through 600000
        run: |
          set -euxo pipefail
          for c in std cedar; do
            /tmp/target-g3/release/sokonanoda /tmp/checker.json </tmp/$c.test.ndjson >/tmp/$c.g3.test.out
            /tmp/target-guard/release/sokonanoda /tmp/checker.json </tmp/$c.test.ndjson >/tmp/$c.guard.test.out
            cmp /tmp/$c.g3.test.out /tmp/$c.guard.test.out
            echo "${c^^}_V16_HELDOUT_BYTE_IDENTICAL=PASS" | tee -a /tmp/result.txt
          done
          echo V16_HELDOUT_ROWS=400001..600000 | tee -a /tmp/result.txt
          echo OPERATOR_ALPHABET_GENESIS_V16=BOUNDED_POSITIVE | tee -a /tmp/result.txt
          cat /tmp/result.txt

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: msikernel-operator-alphabet-genesis-v16
          path: |
            /tmp/result.txt
            /tmp/prereg.md
            /tmp/learn.txt
            /tmp/runtime-probe.txt
            /tmp/v16_observations.csv
            /tmp/*broad.err

name: MSI demand-gated canonicalization v23
on:
  push:
    branches: [msi-canon-demand-gate-v23]
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Install Valgrind
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y valgrind
      - name: Prepare Std prefix and variants
        shell: bash
        run: |
          set -euxo pipefail
          git clone --depth 1 https://github.com/leanprover/lean-kernel-arena.git arena
          cd arena
          nix develop -c ./lka.py build-test std
          head -n 1000000 _build/tests/std.ndjson > /tmp/std-prefix.ndjson
          cd ..

          git clone --depth 1 --branch mathgraph https://github.com/metalogiclabs/mathgraph-lean-kernel.git /tmp/base
          for mode in no_rigid no_unfold no_both if_prev; do
            cp -a /tmp/base /tmp/$mode
            cp experiments/apply_msi_canon_demand_gate_v23.py /tmp/$mode/patch.py
            (cd /tmp/$mode && python3 patch.py $mode)
          done

          cd arena
          for mode in base no_rigid no_unfold no_both if_prev; do
            nix develop -c bash -euxo pipefail -c "CARGO_TARGET_DIR=/tmp/${mode}-target RUSTFLAGS='-C target-cpu=x86-64' cargo build --manifest-path /tmp/${mode}/Cargo.toml --release --locked"
          done
          cat >/tmp/checker.json <<'EOF'
          {"use_stdin":true,"nat_extension":true,"string_extension":true,"unpermitted_axiom_hard_error":false,"unsafe_permit_all_axioms":true,"num_threads":1,"print_success_message":false}
          EOF
      - name: Native tournament and deterministic Callgrind
        shell: bash
        run: |
          set -euxo pipefail
          : > /tmp/result.txt
          variants='base no_rigid no_unfold no_both if_prev'
          for i in 1 2 3; do
            for mode in $variants; do
              /usr/bin/time -f "${mode^^}_NATIVE_${i}=%e RSS=%M" /tmp/${mode}-target/release/sokonanoda /tmp/checker.json < /tmp/std-prefix.ndjson >/tmp/${mode}-${i}.out 2>>/tmp/result.txt
              if [ "$mode" != base ]; then cmp /tmp/base-${i}.out /tmp/${mode}-${i}.out; fi
            done
          done
          for mode in $variants; do
            valgrind --tool=callgrind --callgrind-out-file=/tmp/${mode}.cg /tmp/${mode}-target/release/sokonanoda /tmp/checker.json < /tmp/std-prefix.ndjson >/tmp/${mode}-cg.out 2>/tmp/${mode}-cg.err
            if [ "$mode" != base ]; then cmp /tmp/base-cg.out /tmp/${mode}-cg.out; fi
            grep 'Collected' /tmp/${mode}-cg.err | tail -1 | sed "s/^/${mode^^}_/" | tee -a /tmp/result.txt
          done
          python3 - <<'PY' | tee -a /tmp/result.txt
          import re, statistics
          s=open('/tmp/result.txt').read()
          modes=['base','no_rigid','no_unfold','no_both','if_prev']
          means={}; instr={}
          for m in modes:
              xs=[float(x) for x in re.findall(fr'{m.upper()}_NATIVE_\d+=([0-9.]+)',s)]
              means[m]=statistics.mean(xs)
              mm=re.search(fr'{m.upper()}_.*Collected\s*:\s*([0-9,]+)',s)
              instr[m]=int(mm.group(1).replace(',',''))
              print(f'{m.upper()}_NATIVE_MEAN={means[m]:.6f}')
              print(f'{m.upper()}_INSTR={instr[m]}')
          for m in modes[1:]:
              print(f'{m.upper()}_NATIVE_DELTA_PCT={100*(means[m]-means["base"])/means["base"]:.6f}')
              print(f'{m.upper()}_INSTR_DELTA_PCT={100*(instr[m]-instr["base"])/instr["base"]:.6f}')
          winner=min(modes[1:], key=lambda m: instr[m])
          print(f'BEST_CANDIDATE={winner}')
          print(f'BEST_INSTR_DELTA_PCT={100*(instr[winner]-instr["base"])/instr["base"]:.6f}')
          print('DECISION=' + ('TRANSFER_WINNER' if instr[winner] < instr['base'] else 'KEEP_BASELINE_CANON'))
          PY
          cat /tmp/result.txt
      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: msi-canon-demand-gate-v23
          path: |
            /tmp/result.txt
            /tmp/base-cg.err
            /tmp/no_rigid-cg.err
            /tmp/no_unfold-cg.err
            /tmp/no_both-cg.err
            /tmp/if_prev-cg.err
